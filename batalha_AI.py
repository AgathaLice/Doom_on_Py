class Navio:
    def definir_vida(formato):
        for linhas in formato:
            for itens in linhas:
                if itens == 

    def __init__(self, formato):
        self.formato = formato